# Flow 06: Algolia lexical search
# Data contract: TEST_USER_EMAIL owns at least one indexed transcript matching
# SEARCH_TEST_TERM. CI injects the term; the flow never hardcodes test content.
appId: com.secondbrainlabs.core
tags:
  - critical
  - search

---

- runFlow: utils/reset_app.yaml

- runFlow: utils/ensure_logged_out.yaml

- extendedWaitUntil:
    visible:
      id: "login-email-input"
    timeout: 15000

- runFlow: utils/login.yaml

- tapOn:
    id: "search-tab-button"

- assertVisible:
    text: "Search your library..."

- tapOn:
    id: "search-input"
- inputText: ${SEARCH_TEST_TERM}

- extendedWaitUntil:
    visible:
      id: "search-result-card"
    timeout: 15000

- tapOn:
    id: "search-result-card"
    index: 0

# "AI Artifacts" only renders once the detail polling resolves to a terminal
# state, which is one API round-trip past the screen transition.
- extendedWaitUntil:
    visible:
      text: "AI Artifacts"
    timeout: 45000
